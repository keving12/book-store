pluginManagement {
    plugins {
        id 'io.spring.dependency-management' version "${dependencyManagementPluginVersion}"
    }
}

rootProject.name = 'mytutor-test'

include 'book-sales'
include 'book-reports'
include 'book-store'